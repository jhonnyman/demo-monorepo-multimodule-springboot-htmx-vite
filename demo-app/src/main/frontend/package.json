{
  "name": "@demo.com/app",
  "description": "Demo App module",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "npm run build:vite",
    "build:watch": "vite build --watch",
    "build:vite": "vite build",
    "build:ts": "tsc"
  },
  "dependencies": {
    "@demo.com/vite-ts": "^0.0.1"
  }
}
